# `singlePopulationConsensusTiles`

`singlePopulationConsensusTiles` is an R helper function, part of the
single-cell peak

## Usage

``` r
singlePopulationConsensusTiles(
  peaksExperiment,
  sampleData,
  threshold,
  groupColumn = NULL,
  verbose = TRUE
)
```
