# Get cell population names from a MOCHA object

Get cell population names from a MOCHA object

## Usage

``` r
cellTypes(x)
```

## Arguments

- x:

  A `MochaTileResults` or `MochaSampleTileMatrix` object.

## Value

Character vector of cell population names.
