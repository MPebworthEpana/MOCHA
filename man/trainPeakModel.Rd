% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trainPeakModel.R
\name{trainPeakModel}
\alias{trainPeakModel}
\title{Retrain MOCHA peak-calling models at a chosen tile size}
\usage{
trainPeakModel(
  ATACFragments,
  cellColData,
  blackList,
  groundTruthPeaks = NULL,
  tileSize = .MOCHA_DEFAULT_TILE_SIZE,
  cellCol = "RG",
  sampleColumn = "Sample",
  cellSubsetSizes = c(5, 10, 25, 50, 100, 250, 500, 1000, 2500, 5000, 10000, 25000,
    50000, 1e+05, 170000),
  replicatesFn = function(n) {
     if (n < 50000) 
         10L
     else 5L
 },
  threshMethod = c("youden", "f1", "topleft"),
  smoothMethod = c("auto", "loess", "linear"),
  callPeaks = FALSE,
  macs2Args = list(g = "hs", f = "BED", nolambda = TRUE, shift = -75, extsize = 150,
    broad = TRUE),
  trimPeakBp = 75L,
  numCores = 1L,
  seed = 1L,
  verbose = FALSE
)
}
\arguments{
\item{ATACFragments}{A \code{GRangesList} of single-cell ATAC fragments for
one training cell population (typically pooled across samples).}

\item{cellColData}{Cell metadata; used to compute the training median
fragments per cell when column \code{nFrags} is present.}

\item{blackList}{\code{GRanges} of regions to exclude when tiling.}

\item{groundTruthPeaks}{\code{GRanges} of accessible regions for training
labels. Required unless \code{callPeaks = TRUE}.}

\item{tileSize}{Width of genomic tiles in base pairs. Default \code{500L}.}

\item{cellCol}{Column in fragment \code{mcols} with cell barcodes. Default
\code{"RG"}.}

\item{sampleColumn}{Unused for training labels; reserved for compatibility.}

\item{cellSubsetSizes}{Integer vector of cell counts at which to fit models.}

\item{replicatesFn}{Function of cell count \code{n} returning the number of
training replicates. Default: 10 below 50k cells, 5 otherwise.}

\item{threshMethod}{Method for optimal probability cutoffs per cell count:
\code{"youden"} (requires \pkg{cutpointr}), \code{"f1"}, or \code{"topleft"}.}

\item{smoothMethod}{Coefficient smoothing: \code{"auto"} (LOESS + linear, as
in \code{make_prediction}), \code{"loess"}, or \code{"linear"}.}

\item{callPeaks}{If \code{TRUE}, run MACS2 on pooled pseudobulk fragments to
derive \code{groundTruthPeaks}. Requires \code{macs2} on \code{PATH} and
\pkg{rtracklayer}.}

\item{macs2Args}{Named list of MACS2 arguments passed to
\code{macs2 callpeak} (defaults mirror the MOCHA paper / ArchR settings).}

\item{trimPeakBp}{Base pairs trimmed from each end of broad peaks before
overlay on tiles. Default \code{75L}.}

\item{numCores}{Cores for parallel replicate fitting. Default \code{1L}.}

\item{seed}{Random seed for cell subsampling.}

\item{verbose}{Print progress messages.}
}
\value{
A \code{MOCHAPeakModel} object for use with \code{peakModel} in
  \code{\link{callOpenTiles}}.
}
\description{
Fits cell-count-specific logistic regression models and Youden-optimal
probability thresholds following the MOCHA publication Methods (NK-cell
training on MACS2 pseudo-bulk labels). The returned \code{MOCHAPeakModel}
can be passed to \code{\link{callOpenTiles}} via \code{peakModel}.
}
\details{
Optional dependencies:
\itemize{
  \item \pkg{cutpointr} when \code{threshMethod = "youden"}
  \item \pkg{rtracklayer} and a \code{macs2} binary when \code{callPeaks = TRUE}
}
}
\examples{
\dontrun{
if (requireNamespace("cutpointr", quietly = TRUE)) {
  model <- MOCHA::trainPeakModel(
    ATACFragments = MOCHA::exampleFragments[[1]],
    cellColData = MOCHA::exampleCellColData,
    blackList = MOCHA::exampleBlackList,
    groundTruthPeaks = myPeaks,
    tileSize = 250L,
    cellSubsetSizes = c(50, 100, 500),
    replicatesFn = function(n) 2L,
    numCores = 2
  )
  tiles <- MOCHA::callOpenTiles(
    ATACFragments = MOCHA::exampleFragments,
    cellColData = MOCHA::exampleCellColData,
    blackList = MOCHA::exampleBlackList,
    genome = "hg19",
    TxDb = "TxDb.Hsapiens.UCSC.hg38.refGene",
    OrgDb = "org.Hs.eg.db",
    outDir = tempdir(),
    cellPopLabel = "Clusters",
    cellPopulations = "C2",
    peakModel = model,
    numCores = 1
  )
}
}

}
