% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assessDropout.R
\name{assessDropout}
\alias{assessDropout}
\title{Assess technical vs biological zeros on a sample-tile matrix}
\usage{
assessDropout(
  TSAM_Object,
  model = NULL,
  cellPopulation = "all",
  techThreshold = 0.2,
  bioThreshold = 0.8,
  verbose = FALSE
)
}
\arguments{
\item{TSAM_Object}{A SummarizedExperiment from \code{\link{getSampleTileMatrix}}.}

\item{model}{Optional \code{DropoutModel} from \code{estimateDropoutModel}. If
\code{NULL}, a model is estimated with default settings.}

\item{cellPopulation}{Cell population name(s) to assess, or \code{"all"} for
every population in \code{model}. When estimating a new model, only these
populations are fitted.}

\item{techThreshold}{Maximum \code{P(zero)} for classifying an observed zero as
technical (unexpected). Default is 0.2.}

\item{bioThreshold}{Minimum \code{P(zero)} for classifying an observed zero as
biological (expected closure). Default is 0.8.}

\item{verbose}{Logical. Default is \code{FALSE}.}
}
\value{
The input \code{TSAM_Object} with \code{DropoutProb_*} assays added.
}
\description{
\code{assessDropout} fits a dropout model and stores predicted
  \code{P(zero)} per tile and sample in \code{DropoutProb_<CellPop>} assays.
  **Lower** values indicate a zero is more likely technical (unexpected,
  given tile signal and depth). Model objects are saved in
  \code{metadata(TSAM)$dropoutModels}.
}
\examples{
\dontrun{
TSAM_with_dropout <- MOCHA::assessDropout(SampleTileMatrices)
probs <- MOCHA::getDropoutProb(TSAM_with_dropout, "CD16 Mono")
}

}
\keyword{downstream}
